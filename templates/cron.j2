# This file was generated by Ansible

# Run backups and purges
{% for profile in profiles %}
{% if profile.schedule|default(None) %}
{{profile.schedule}} {{profile.user|default(backup.user)}} /bin/bash /usr/bin/duply {{backup.home}}/{{profile.name}} {{profile.action|default('backup')}} >> {{backup.logdir}}/{{profile.name}}.log 2>&1 && /bin/bash /usr/bin/duply {{backup.home}}/{{profile.name}} {{profile.action|default('purgeFull --force')}} >> {{backup.logdir}}/{{profile.name}}.log 2>&1 && /bin/bash /usr/bin/duply {{backup.home}}/{{profile.name}} {{profile.action|default('purgeIncr --force')}} >> {{backup.logdir}}/{{profile.name}}.log 2>&1
{% endif %}
{% endfor %}

